# Logic Lab

A visual, progressive learning tool for Python Boolean logic. It introduces `True`/`False`, `and`, `or`, and `not` with interactive toggles, live results, and short unlockable challenges.

## Run locally

```powershell
python server.py
```

Then visit `http://localhost:8000`. The server will try to open the page automatically.

No packages or installation are required.
